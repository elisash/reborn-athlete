import React from "react";

export default function Roster({ user }) {
  return (
    <div className="roster">
      Roster for your game on
      {/* {event.date} at {event.time} in {event.location} */}
    </div>
  );
}
